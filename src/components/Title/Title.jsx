import styled from "styled-components";

const Header = styled.h1`
  color: rgb(211, 211, 211);
  text-align: center;
`;

const Title = () => {
  return <Header>Todo-application</Header>;
};

export default Title;
